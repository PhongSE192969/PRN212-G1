<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- PALETTE NHẸ -->
    <SolidColorBrush x:Key="Chk.Primary" Color="#FF6A00"/>
    <SolidColorBrush x:Key="Chk.PrimaryDark" Color="#E35D00"/>
    <SolidColorBrush x:Key="Chk.Success" Color="#18A965"/>
    <SolidColorBrush x:Key="Chk.Surface" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="Chk.Border" Color="#E9E9E9"/>
    <SolidColorBrush x:Key="Chk.Text" Color="#2B2B2B"/>
    <SolidColorBrush x:Key="Chk.Muted" Color="#6F6F6F"/>
    <CornerRadius   x:Key="Chk.Radius">12</CornerRadius>

    <!-- THẺ (tổng tiền / box) -->
    <Style x:Key="Chk.Card" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Chk.Surface}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Chk.Radius}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Chk.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="14"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="12" ShadowDepth="1" Opacity="0.12"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- NÚT CHUNG -->
    <Style x:Key="Chk.Btn" TargetType="Button">
        <Setter Property="Padding" Value="14,10"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="{StaticResource Chk.Primary}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border CornerRadius="{StaticResource Chk.Radius}"
                  Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource Chk.PrimaryDark}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Chk.BtnSuccess" BasedOn="{StaticResource Chk.Btn}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Chk.Success}"/>
    </Style>

    <!-- TEXTBOX MÃ GIẢM -->
    <Style x:Key="Chk.Input" TargetType="TextBox">
        <Setter Property="Padding" Value="10"/>
        <Setter Property="BorderBrush" Value="{StaticResource Chk.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border CornerRadius="{StaticResource Chk.Radius}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  Background="{TemplateBinding Background}">
                        <ScrollViewer x:Name="PART_ContentHost"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource Chk.Primary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- DATAGRID GIỎ HÀNG TINH GỌN -->
    <Style x:Key="Chk.Grid" TargetType="DataGrid">
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="BorderBrush" Value="{StaticResource Chk.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="RowBackground" Value="#FFF"/>
        <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
    </Style>

</ResourceDictionary>
