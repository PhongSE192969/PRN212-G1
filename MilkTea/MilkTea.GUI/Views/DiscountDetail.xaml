<Window x:Class="MilkTea.GUI.Views.DiscountDetail"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chi tiết mã giảm giá"
        Height="300" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Mã:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtCode" Grid.Row="0" Grid.Column="1" Margin="5,2"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Mô tả:" VerticalAlignment="Top" Margin="0,5,0,0"/>
            <TextBox x:Name="txtDescription"
                     Grid.Row="1" Grid.Column="1"
                     Margin="5,5,0,0"
                     Height="60"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Phần trăm (%):" VerticalAlignment="Center" Margin="0,5,0,0"/>
            <TextBox x:Name="txtPercentage" Grid.Row="2" Grid.Column="1" Margin="5,5,0,0"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Ngày hết hạn:" VerticalAlignment="Center" Margin="0,5,0,0"/>
            <DatePicker x:Name="dpExpireDate" Grid.Row="3" Grid.Column="1" Margin="5,5,0,0"/>
        </Grid>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <!-- CHỈ HIỆN KHI THÊM -->
            <Button x:Name="btnAdd"
                    Content="Thêm"
                    Width="90" Height="30" Margin="5"
                    Click="BtnAdd_Click"/>

            <!-- CHỈ HIỆN KHI SỬA -->
            <Button x:Name="btnUpdate"
                    Content="Lưu"
                    Width="90" Height="30" Margin="5"
                    Click="BtnUpdate_Click"/>

            <Button Content="Hủy"
                    Width="90" Height="30" Margin="5"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
