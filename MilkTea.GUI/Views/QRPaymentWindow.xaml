<Window x:Class="MilkTea.GUI.Views.QRPaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thanh toán bằng QR Code" 
        Height="650" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF6B35" Padding="20">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="📱 Thanh toán QR Code" 
                           FontSize="22" 
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Quét mã để thanh toán" 
                           FontSize="14" 
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- QR Code Display -->
        <Border Grid.Row="1" 
                Background="White" 
                Margin="20"
                CornerRadius="10"
                Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
            </Border.Effect>
            
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <!-- QR Code Image -->
                <Border BorderBrush="#E0E0E0" 
                        BorderThickness="2" 
                        CornerRadius="5"
                        Padding="15"
                        Background="White">
                    <Image x:Name="imgQRCode" 
                           Width="350" 
                           Height="350"
                           Stretch="Uniform"/>
                </Border>
                
                <!-- Payment Info -->
                <TextBlock x:Name="txtAmount"
                           Text="Tổng tiền: 0 đ"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="#FF6B35"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,0"/>
                
                <TextBlock Text="Quét mã QR bằng ứng dụng MoMo hoặc Banking"
                           FontSize="12"
                           Foreground="Gray"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="20,10,20,20">
            <Button Content="✅ Đã thanh toán"
                    Width="180"
                    Height="50"
                    FontSize="15"
                    FontWeight="Bold"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Margin="0,0,10,0"
                    Click="BtnConfirm_Click"/>
            
            <Button Content="❌ Hủy"
                    Width="180"
                    Height="50"
                    FontSize="15"
                    FontWeight="Bold"
                    Background="#F44336"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
