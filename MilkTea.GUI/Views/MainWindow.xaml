<Window x:Class="MilkTea.GUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TeaPOS - Hệ Thống Thu Ngân" 
        Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF6B35" Padding="20,15">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🧋 TeaPOS" 
                               FontSize="24" 
                               FontWeight="Bold"
                               Foreground="White"/>
                    <TextBlock x:Name="txtWelcome"
                               Text="Xin chào, User"
                               FontSize="16"
                               Foreground="White"
                               VerticalAlignment="Center"
                               Margin="30,0,0,0"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <Button x:Name="btnCategoryManagement"
                            Content="📋 Danh mục"
                            Style="{StaticResource ModernButton}"
                            Background="#9C27B0"
                            Margin="0,0,10,0"
                            Click="BtnCategoryManagement_Click"
                            Visibility="Collapsed"/>
                    <Button Content="📊 Báo cáo"
                            Style="{StaticResource ModernButton}"
                            Background="#4CAF50"
                            Margin="0,0,10,0"
                            Click="BtnReport_Click"/>
                    <Button Content="🚪 Đăng xuất"
                            Style="{StaticResource ModernButton}"
                            Background="#F44336"
                            Click="BtnLogout_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Products -->
            <Border Grid.Column="0" 
                    Background="White" 
                    CornerRadius="10" 
                    Padding="15"
                    Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Title -->
                    <TextBlock Grid.Row="0"
                               Text="📋 Danh sách sản phẩm"
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="0,0,0,15"/>

                    <!-- Categories -->
                    <ScrollViewer Grid.Row="1" 
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Disabled"
                                  Margin="0,0,0,15">
                        <ItemsControl x:Name="lstCategories">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding CategoryName}"
                                            Tag="{Binding CategoryId}"
                                            Style="{StaticResource ModernButton}"
                                            Margin="0,0,10,0"
                                            Click="BtnCategory_Click"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Products Grid -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="lstProducts">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White"
                                            BorderBrush="#E0E0E0"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Width="180"
                                            Height="220"
                                            Margin="10"
                                            Cursor="Hand"
                                            MouseLeftButtonUp="ProductCard_Click"
                                            Tag="{Binding ProductId}">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                                        </Border.Effect>
                                        
                                        <StackPanel Margin="10">
                                            <!-- Image Placeholder -->
                                            <Border Background="#FFF3E0"
                                                    Height="120"
                                                    CornerRadius="5"
                                                    Margin="0,0,0,10">
                                                <TextBlock Text="🧋"
                                                           FontSize="48"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                            </Border>
                                            
                                            <TextBlock Text="{Binding ProductName}"
                                                       FontWeight="SemiBold"
                                                       FontSize="14"
                                                       TextWrapping="Wrap"
                                                       Height="40"
                                                       Margin="0,0,0,5"/>
                                            
                                            <TextBlock FontSize="16"
                                                       FontWeight="Bold"
                                                       Foreground="#FF6B35">
                                                <Run Text="{Binding Price, StringFormat=N0}"/>
                                                <Run Text="đ"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Right Panel: Cart -->
            <Border Grid.Column="1" 
                    Background="White" 
                    CornerRadius="10" 
                    Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Cart Title -->
                    <TextBlock Grid.Row="0"
                               Text="🛒 Giỏ hàng"
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="0,0,0,15"/>

                    <!-- Cart Items -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="lstCart">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0"
                                            BorderThickness="0,0,0,1"
                                            Padding="0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding DisplayName, Mode=OneWay}"
                                                           FontWeight="SemiBold"
                                                           TextWrapping="Wrap"/>
                                                <TextBlock Margin="0,5,0,0">
                                                    <Run Text="{Binding Quantity, Mode=OneWay}"/>
                                                    <Run Text="x"/>
                                                    <Run Text="{Binding UnitPrice, Mode=OneWay, StringFormat=N0}"/>
                                                    <Run Text="đ"/>
                                                </TextBlock>
                                                <TextBlock FontWeight="Bold" Foreground="#FF6B35">
                                                    <Run Text="{Binding Subtotal, Mode=OneWay, StringFormat=N0}"/>
                                                    <Run Text="đ"/>
                                                </TextBlock>
                                            </StackPanel>
                                            
                                            <Button Grid.Column="1"
                                                    Content="✕"
                                                    Width="30"
                                                    Height="30"
                                                    Background="#F44336"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    FontWeight="Bold"
                                                    Cursor="Hand"
                                                    Click="BtnRemoveItem_Click"
                                                    Tag="{Binding}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- Discount Code -->
                    <StackPanel Grid.Row="2" Margin="0,15,0,0">
                        <TextBlock Text="Mã giảm giá:" FontSize="14" Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="txtDiscountCode"
                                     Grid.Column="0"
                                     Style="{StaticResource ModernTextBox}"
                                     Height="35"/>
                            <Button Grid.Column="1"
                                    Content="Áp dụng"
                                    Style="{StaticResource ModernButton}"
                                    Width="80"
                                    Height="35"
                                    Margin="5,0,0,0"
                                    Click="BtnApplyDiscount_Click"/>
                        </Grid>
                        <TextBlock x:Name="txtDiscountInfo"
                                   Text=""
                                   Foreground="Green"
                                   FontSize="12"
                                   Margin="0,5,0,0"/>
                    </StackPanel>

                    <!-- Totals & Checkout -->
                    <StackPanel Grid.Row="3" Margin="0,20,0,0">
                        <Border BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="0,15,0,0">
                            <StackPanel>
                                <!-- Subtotal -->
                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Tạm tính:" FontSize="14"/>
                                    <TextBlock x:Name="txtSubtotal" 
                                               Text="0 đ"
                                               FontSize="14"
                                               HorizontalAlignment="Right"/>
                                </Grid>
                                
                                <!-- VAT -->
                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="VAT (10%):" FontSize="14"/>
                                    <TextBlock x:Name="txtVAT"
                                               Text="0 đ"
                                               FontSize="14"
                                               HorizontalAlignment="Right"/>
                                </Grid>
                                
                                <!-- Discount -->
                                <Grid Margin="0,0,0,8">
                                    <TextBlock Text="Giảm giá:" FontSize="14"/>
                                    <TextBlock x:Name="txtDiscount"
                                               Text="0 đ"
                                               FontSize="14"
                                               Foreground="Red"
                                               HorizontalAlignment="Right"/>
                                </Grid>
                                
                                <!-- Total -->
                                <Grid Margin="0,10,0,15">
                                    <TextBlock Text="TỔNG CỘNG:" 
                                               FontSize="16" 
                                               FontWeight="Bold"/>
                                    <TextBlock x:Name="txtTotal"
                                               Text="0 đ"
                                               FontSize="20"
                                               FontWeight="Bold"
                                               Foreground="#FF6B35"
                                               HorizontalAlignment="Right"/>
                                </Grid>
                                
                                <!-- Checkout Button -->
                                <Button Content="� Thanh toán"
                                        Style="{StaticResource ModernButton}"
                                        Background="#FF6B35"
                                        Height="50"
                                        FontSize="16"
                                        FontWeight="Bold"
                                        Click="BtnCheckout_Click"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
