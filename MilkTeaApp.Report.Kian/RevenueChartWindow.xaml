<Window x:Class="MilkTeaApp.Report.Kian.RevenueChartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
        Title="Biểu đồ Doanh thu theo Tháng"
        Height="500" Width="750"
        WindowStartupLocation="CenterScreen"
        Background="#FFF9F6F2">

    <Grid Margin="20">
        <TextBlock Text="📊 Biểu đồ Doanh thu theo Tháng"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="#4E342E"
                   Margin="0,0,0,15"/>

        <oxy:PlotView x:Name="RevenuePlot"
                      Margin="0,50,0,0"
                      Height="380"/>
    </Grid>
</Window>
